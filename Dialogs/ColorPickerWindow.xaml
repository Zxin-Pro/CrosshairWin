<!--
    Colour picker dialog.

    Matches the settings window's visual language: dark surfaces, rounded cards,
    accent-blue highlights. WPF has no built-in colour dialog, so this keeps the
    dependency surface at zero while looking native to the rest of the app.

    All x:Name fields and handlers used by ColorPickerWindow.xaml.cs are preserved:
    HexBox, RSlider, GSlider, BSlider, ASlider, SwatchBorder, PresetPanel.
-->
<Window x:Class="CrosshairWin.Dialogs.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择颜色"
        Width="460" Height="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FF14161A"
        Foreground="#FFE6E8EC"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        FontSize="13"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">

    <Window.Resources>
        <SolidColorBrush x:Key="Accent" Color="#FF4EA1FF" />
        <SolidColorBrush x:Key="AccentDim" Color="#FF2B6CB8" />
        <SolidColorBrush x:Key="Surface1" Color="#FF1C1F25" />
        <SolidColorBrush x:Key="Surface2" Color="#FF23272F" />
        <SolidColorBrush x:Key="Surface3" Color="#FF2C313A" />
        <SolidColorBrush x:Key="Hairline" Color="#FF343946" />
        <SolidColorBrush x:Key="TextPrimary" Color="#FFE6E8EC" />
        <SolidColorBrush x:Key="TextSecondary" Color="#FF9BA3B0" />
        <SolidColorBrush x:Key="TextMuted" Color="#FF6B7280" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Background" Value="{StaticResource Surface3}" />
            <Setter Property="BorderBrush" Value="{StaticResource Hairline}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="MinWidth" Value="86" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF3A4150" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentDim}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF2A3038" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentDim}" />
            <Setter Property="BorderBrush" Value="{StaticResource Accent}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="CaretBrush" Value="{StaticResource Accent}" />
            <Setter Property="SelectionBrush" Value="{StaticResource Accent}" />
            <Setter Property="FontFamily" Value="Consolas, Cascadia Mono, Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{StaticResource Surface2}"
                                BorderBrush="{StaticResource Hairline}" BorderThickness="1"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="IsMoveToPointEnabled" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center" Height="20">
                            <Border Height="6" CornerRadius="3" Background="{StaticResource Surface3}"
                                    VerticalAlignment="Center" />
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="6" CornerRadius="3"
                                                        Background="{StaticResource Accent}"
                                                        VerticalAlignment="Center" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" Height="6" VerticalAlignment="Center" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="14" Height="14" Focusable="False">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Grid>
                                                    <Ellipse Fill="#FF0E1116" Width="14" Height="14" />
                                                    <Ellipse x:Name="Knob" Fill="White" Width="10" Height="10" />
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Knob" Property="Fill" Value="{StaticResource Accent}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="十六进制颜色（#RRGGBB 或 #AARRGGBB）"
                   Foreground="{StaticResource TextSecondary}"
                   FontSize="12"
                   Margin="0,0,0,7" />

        <TextBox Grid.Row="1" x:Name="HexBox" Margin="0,0,0,16"
                 TextChanged="HexBox_TextChanged" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- channel sliders -->
            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                <Grid Margin="0,0,0,2">
                    <TextBlock Text="R" Foreground="{StaticResource TextMuted}" FontSize="11.5" />
                </Grid>
                <Slider x:Name="RSlider" Minimum="0" Maximum="255" ValueChanged="Channel_Changed" Margin="0,0,0,10" />

                <TextBlock Text="G" Foreground="{StaticResource TextMuted}" FontSize="11.5" Margin="0,0,0,2" />
                <Slider x:Name="GSlider" Minimum="0" Maximum="255" ValueChanged="Channel_Changed" Margin="0,0,0,10" />

                <TextBlock Text="B" Foreground="{StaticResource TextMuted}" FontSize="11.5" Margin="0,0,0,2" />
                <Slider x:Name="BSlider" Minimum="0" Maximum="255" ValueChanged="Channel_Changed" Margin="0,0,0,10" />

                <TextBlock Text="A" Foreground="{StaticResource TextMuted}" FontSize="11.5" Margin="0,0,0,2" />
                <Slider x:Name="ASlider" Minimum="0" Maximum="255" ValueChanged="Channel_Changed" />
            </StackPanel>

            <!-- swatch -->
            <Border Grid.Column="1" Width="96" Margin="16,0"
                    BorderBrush="{StaticResource Hairline}" BorderThickness="1"
                    CornerRadius="8" ClipToBounds="True">
                <Grid>
                    <Rectangle Opacity="0.08">
                        <Rectangle.Fill>
                            <DrawingBrush TileMode="Tile" Viewport="0,0,12,12" ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <DrawingGroup>
                                        <GeometryDrawing Brush="#FFFFFFFF">
                                            <GeometryDrawing.Geometry>
                                                <RectangleGeometry Rect="0,0,12,12" />
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                        <GeometryDrawing Brush="#FF000000">
                                            <GeometryDrawing.Geometry>
                                                <GeometryGroup>
                                                    <RectangleGeometry Rect="0,0,6,6" />
                                                    <RectangleGeometry Rect="6,6,6,6" />
                                                </GeometryGroup>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                    </DrawingGroup>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <Border x:Name="SwatchBorder" Background="Red" />
                </Grid>
            </Border>

            <!-- presets -->
            <StackPanel Grid.Column="2" Margin="4,0,0,0">
                <TextBlock Text="预设颜色" Foreground="{StaticResource TextSecondary}"
                           FontSize="12" Margin="0,0,0,10" />
                <WrapPanel x:Name="PresetPanel" />
                <TextBlock Text="点击色块快速选择"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="11"
                           TextWrapping="Wrap"
                           Margin="0,10,0,0" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="取消" Click="Cancel_Click" />
            <Button Content="确定" Style="{StaticResource PrimaryButton}" Click="Ok_Click" Margin="0" />
        </StackPanel>
    </Grid>
</Window>
